**Description**: A script that checks the firmware version of all NIC devices on a system. Excludes built-in NICs and Loopback device. 

**Usage**: `./getFW.sh`  
*NOTE:* Verify script is executable using `chmod +x getFW.sh`
